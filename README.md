# Process environment enactment

Private hosted-runner proof for native process environment carriers.

- POSIX: `execve(..., envp)` on Ubuntu and macOS hosted runners.
- Windows: `CreateProcessW(..., lpEnvironment, ...)` on a Windows hosted runner.

Created by Codex on 2026-06-09T17:31:35Z for durable evidence backfill.
